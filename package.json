{
  "name": "ai-usage-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension that tracks your usage limits across Claude, ChatGPT, and Gemini.",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/popup dist/options && cp src/popup/popup.html dist/popup/popup.html && cp src/options/options.html dist/options/options.html",
    "watch": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && node --test dist-test"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.4"
  }
}
