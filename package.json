{
  "name": "usage-monitor",
  "version": "0.3.0",
  "private": true,
  "description": "Browser extension that checks your Claude.ai usage percentage on demand, with optional Discord/Telegram/Slack alerts.",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/popup dist/options && cp src/popup/popup.html dist/popup/popup.html && cp src/options/options.html dist/options/options.html",
    "watch": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && node --test dist-test",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.4"
  }
}
